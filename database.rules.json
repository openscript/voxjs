{
  "rules": {
    "replies" : {
      ".read": "true",
      "$reply_id": {
        ".write": "auth !== null && !data.exists() && newData.exists()",
        "message": {
          ".validate": true
        },
        "$other": {
          ".validate": false
        }
      }
    }
  }
}